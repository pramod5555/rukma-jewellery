<section class="rukma-hero full-width">
  <div class="rukma-hero__grid">
    <div class="rukma-hero__pillar rukma-hero__pillar--left">
      {%- if section.settings.pillar_left_image -%}
        {{ section.settings.pillar_left_image
          | image_url: width: 800
          | image_tag: loading: 'eager', class: 'rukma-hero__pillar-img'
        }}
      {%- else -%}
        {{ 'collection-1' | placeholder_svg_tag: 'rukma-hero__pillar-img rukma-hero__placeholder' }}
      {%- endif -%}
    </div>

    <div class="rukma-hero__center">
      {%- if section.settings.video_url != blank -%}
        <video
          class="rukma-hero__bg"
          src="{{ section.settings.video_url }}"
          autoplay
          muted
          loop
          playsinline
        ></video>
      {%- elsif section.settings.hero_image -%}
        {{ section.settings.hero_image
          | image_url: width: 1600
          | image_tag: loading: 'eager', class: 'rukma-hero__bg'
        }}
      {%- else -%}
        {{ 'lifestyle-1' | placeholder_svg_tag: 'rukma-hero__bg rukma-hero__placeholder rukma-hero__bg--placeholder' }}
      {%- endif -%}

      {%- if section.settings.eyebrow != blank or section.settings.heading != blank or section.settings.cta_label != blank -%}
        <div class="rukma-hero__copy">
        {%- if section.settings.eyebrow != blank -%}
          <p class="rukma-hero__eyebrow">{{ section.settings.eyebrow }}</p>
        {%- endif -%}
        {%- if section.settings.heading != blank -%}
          <h1 class="rukma-hero__heading">{{ section.settings.heading }}</h1>
        {%- endif -%}
        {%- if section.settings.cta_label != blank and section.settings.cta_url != blank -%}
          <a href="{{ section.settings.cta_url }}" class="rukma-hero__cta">
            {{- section.settings.cta_label -}}
          </a>
        {%- endif -%}
        </div>
      {%- endif -%}
    </div>

    <div class="rukma-hero__pillar rukma-hero__pillar--right">
      {%- if section.settings.pillar_right_image -%}
        {{ section.settings.pillar_right_image
          | image_url: width: 800
          | image_tag: loading: 'eager', class: 'rukma-hero__pillar-img'
        }}
      {%- else -%}
        {{ 'collection-2' | placeholder_svg_tag: 'rukma-hero__pillar-img rukma-hero__placeholder' }}
      {%- endif -%}
    </div>
  </div>

  {%- if section.settings.statement != blank -%}
    <div class="rukma-hero__statement">
      <p>{{ section.settings.statement }}</p>
    </div>
  {%- endif -%}
</section>

{% stylesheet %}
  .rukma-hero {
    background-color: var(--color-bg);
  }

  .rukma-hero__grid {
    position: relative;
    min-height: calc(100svh - 4.8rem);
    overflow: hidden;
  }

  .rukma-hero__pillar {
    position: absolute;
    top: 0;
    bottom: 0;
    z-index: 2;
    width: 120px;
    background-color: rgba(237, 230, 216, 0.78);
    overflow: hidden;
    pointer-events: none;
  }

  .rukma-hero__pillar--left {
    left: 0;
  }

  .rukma-hero__pillar--right {
    right: 0;
  }

  .rukma-hero__pillar-img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
  }

  .rukma-hero__pillar--right .rukma-hero__pillar-img {
    transform: scaleX(-1);
  }

  .rukma-hero__center {
    position: absolute;
    inset: 0;
    min-width: 0;
    min-height: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
    background-color: #1a0a08;
  }

  .rukma-hero__bg {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
  }

  .rukma-hero__bg--placeholder {
    background-color: var(--color-surface);
    color: rgba(44, 36, 32, 0.24);
  }

  .rukma-hero__placeholder {
    width: 100%;
    height: 100%;
  }

  .rukma-hero__placeholder path,
  .rukma-hero__placeholder rect,
  .rukma-hero__placeholder circle {
    vector-effect: non-scaling-stroke;
  }

  .rukma-hero__copy {
    position: absolute;
    left: 3rem;
    right: 3rem;
    bottom: 3rem;
    z-index: 3;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1rem;
    text-align: center;
  }

  .rukma-hero__eyebrow {
    font-family: var(--font-body);
    font-size: 0.6rem;
    letter-spacing: var(--letter-spacing-label);
    text-transform: uppercase;
    color: #F5EDE0;
    opacity: 0.75;
  }

  .rukma-hero__heading {
    font-family: var(--font-heading);
    font-weight: 300;
    font-style: italic;
    font-size: clamp(2.4rem, 4vw, 4rem);
    line-height: 1.0;
    letter-spacing: 0.05em;
    color: #F5EDE0;
  }

  .rukma-hero__cta {
    display: inline-block;
    font-family: var(--font-body);
    font-size: 0.6rem;
    letter-spacing: var(--letter-spacing-label);
    text-transform: uppercase;
    color: #F5EDE0;
    border-bottom: 1px solid rgba(245, 237, 224, 0.6);
    padding-bottom: 0.3rem;
    align-self: flex-start;
    transition: opacity 0.2s;
  }

  .rukma-hero__cta:hover {
    opacity: 0.65;
  }

  .rukma-hero__statement {
    display: grid;
    place-items: center;
    min-height: 42vh;
    padding: 120px var(--page-margin);
    text-align: center;
  }

  .rukma-hero__statement p {
    max-width: 16ch;
    font-family: var(--font-heading);
    font-style: italic;
    font-weight: 300;
    font-size: clamp(3rem, 6vw, 5rem);
    line-height: 0.95;
    letter-spacing: 0.05em;
    color: var(--color-text);
  }

  @media (max-width: 768px) {
    .rukma-hero__grid {
      min-height: 60vh;
    }

    .rukma-hero__pillar {
      display: none;
    }

    .rukma-hero__center {
      min-height: 100%;
    }

    .rukma-hero__copy {
      left: 1.5rem;
      right: 1.5rem;
      bottom: 2rem;
    }

    .rukma-hero__statement {
      min-height: auto;
      padding: 96px var(--page-margin);
    }
  }
{% endstylesheet %}

{% schema %}
{
  "name": "t:sections.rukma_hero.name",
  "settings": [
    {
      "type": "header",
      "content": "t:sections.rukma_hero.pillar_images"
    },
    {
      "type": "image_picker",
      "id": "pillar_left_image",
      "label": "t:sections.rukma_hero.pillar_left"
    },
    {
      "type": "image_picker",
      "id": "pillar_right_image",
      "label": "t:sections.rukma_hero.pillar_right"
    },
    {
      "type": "header",
      "content": "t:sections.rukma_hero.centre_content"
    },
    {
      "type": "text",
      "id": "video_url",
      "label": "t:sections.rukma_hero.video_url"
    },
    {
      "type": "image_picker",
      "id": "hero_image",
      "label": "t:sections.rukma_hero.hero_image"
    },
    {
      "type": "header",
      "content": "t:sections.rukma_hero.copy"
    },
    {
      "type": "text",
      "id": "eyebrow",
      "label": "t:sections.rukma_hero.eyebrow",
      "default": "Heritage jewellery"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "t:sections.rukma_hero.heading",
      "default": ""
    },
    {
      "type": "text",
      "id": "cta_label",
      "label": "t:sections.rukma_hero.cta_label",
      "default": "Explore the collection"
    },
    {
      "type": "url",
      "id": "cta_url",
      "label": "t:sections.rukma_hero.cta_url"
    },
    {
      "type": "text",
      "id": "statement",
      "label": "t:sections.rukma_hero.statement",
      "default": "Heritage jewellery for the contemporary Indian"
    }
  ],
  "presets": [
    {
      "name": "t:sections.rukma_hero.name"
    }
  ]
}
{% endschema %}
